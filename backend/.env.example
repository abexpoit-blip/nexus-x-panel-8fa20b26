# NexusX Backend Configuration
# Copy this to .env on your VPS and fill in real values

# Server
PORT=4000
NODE_ENV=production

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-long-random-string

# Database
DB_PATH=./data/nexus.db

# CORS — your frontend origin
CORS_ORIGIN=https://yourdomain.com

# Default admin (created on first DB init)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-strong-password

# AccHub Provider
ACCHUB_BASE_URL=https://acchub.io/api
ACCHUB_API_KEY=user_xxxxxxxxxxxxxxx_xxxxxx
ACCHUB_USERNAME=
ACCHUB_PASSWORD=

# IMS — manual mode (no API key needed for now)
IMS_MANUAL_MODE=true

# OTP polling interval (seconds)
OTP_POLL_INTERVAL=5

# Rate limit
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
